Declaration means you declare that this function, class, or object will exist. Definition defines what was declared.

Often times, it is best to declare things in a header file and then define it in C++ files.